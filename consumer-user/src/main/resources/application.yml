spring:
  application:
    name: consumer-user
server:
  port: 8200

eureka:
  instance:
    prefer-ip-address: true   #使用服务的ip地址注册服务
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
